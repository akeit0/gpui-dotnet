[package]
name = "gpui-abi-gen"
version = "0.1.0"
edition = "2024"
publish = false

# This tool must be runnable without compiling GPUI or initializing its submodules.
[workspace]

[dependencies]
syn = { version = "=2.0.119", features = ["full", "parsing"] }
