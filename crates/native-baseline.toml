# This metadata mirrors the gpui-kit submodule gitlink and the exact GPUI revision validated
# in Cargo.lock. Update it only as part of a native baseline change that also updates
# docs/UPSTREAM_BASELINE.md and verifies the dependency graph.

[gpui-kit]
fork = "https://github.com/akeit0/gpui-kit"
upstream = "https://github.com/longbridge/gpui-kit"
rev = "c523081c3e4256c68910aa0aa849992d6f62ded4"
upstream_base_rev = "c4d95f2ce97ff5974adba670a27929a345ded1fb"

[gpui-pre]
source = "crates-io"
version = "0.3.4"
