[package]
name = "gpui-dotnet-default-host"
version.workspace = true
edition = "2024"
publish = false

[lib]
name = "gpui_dotnet"
crate-type = ["cdylib"]

[dependencies]
gpui-dotnet = { path = "../.." }

[lints]
workspace = true
